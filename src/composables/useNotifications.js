// Bu composable endi faqat AppLayout.vue da bir marta chaqiriladi
// Polling singleton — store ichida boshqariladi
export function useNotifications() {
  // Bo'sh — store.startPolling() ni AppLayout da onMounted ichida chaqiring
}